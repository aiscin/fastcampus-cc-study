# Clip 9: 포트폴리오 수정 — 대화로 디자인 다듬기

> Part 03 / Ch 04 / 실습 12 / ~20분
> 패턴: 자동 셋업 → 영상 보면서 같이 따라하기 → "완료" → WRAP
> 대응 클립: `30-practice-design/part-03-체험하기/clip-09-포트폴리오수정.md`
> 모범답안: `50-my-work/Part03-체험하기/실습12-포트폴리오수정/p3-c08-따라하기.md`

---

## 자동 셋업 (진행 안내 출력 직전 스킬이 즉시 Bash로 실행)

```bash
# 1) 워크스페이스 루트 자동 감지
WS="$(pwd)"
while [ "$WS" != "/" ] && [ ! -f "$WS/CLAUDE.md" ]; do WS="$(dirname "$WS")"; done
if [ "$WS" = "/" ]; then
  WS="$(pwd)"
  while [ "$WS" != "/" ] && [ ! -d "$WS/40-mock-data" ]; do WS="$(dirname "$WS")"; done
fi
[ "$WS" = "/" ] && { echo "⚠ 워크스페이스 루트 못 찾음"; exit 1; }
echo "워크스페이스: $WS"

# 2) 경로
DEST="$WS/50-my-work/Part03-체험하기/실습12-포트폴리오수정"
SRC="$WS/50-my-work/Part03-체험하기/실습11-포트폴리오기획"

mkdir -p "$DEST"
cd "$DEST"

# 3) Clip 8 결과물(portfolio.html + 00-리서치 + 01-PRD) 통째 복사
if [ -d "$SRC" ]; then
  cp -r "$SRC"/* ./ 2>/dev/null
  rm -f README.md
fi

echo "=== 결과 ==="
ls -la
```

**셋업 결과 한 줄 안내**:
- portfolio.html + 리서치/PRD 다 있음 → "✓ 실습12-포트폴리오수정 폴더 준비 완료. Clip 8에서 만든 portfolio.html + 리서치/PRD 가져왔어요."
- 일부 또는 전체 누락 → "⚠ Clip 8 결과물 일부 또는 전체 없네요. 먼저 Clip 8(포트폴리오 기획)부터 진행해주세요."

---

## 진행 안내 (스킬이 한 메시지로 출력)

```
Clip 9 — 포트폴리오 수정을 시작합니다.

■ 오늘 할 거
Clip 8의 1차 portfolio.html →
  • 5 카테고리(색상·여백·타이포·애니메이션·반응형) 중 3+ 적용
  • portfolio-final.html (다듬기 완료본)
한 사이클 돌립니다.

■ 진행 방식
영상에서 STEP 1~5 순서대로 실습이 진행됩니다.
영상 일시정지·재생 자유롭게 하면서 본인 워크스페이스에서 같이 따라하세요.

■ STEP 흐름

  STEP 1. 작업 폴더 진입 (스킬이 폴더+portfolio.html+리서치/PRD 자동 준비 완료)
    이미 셋업된 폴더: 50-my-work/Part03-체험하기/실습12-포트폴리오수정/
    그대로 이 채팅창에서 STEP 2부터 진행하면 됨.

  STEP 2. 1단계 질문하기 (B) — "어떻게 다듬을 수 있어?"
    "이 1차 포트폴리오(portfolio.html)를 더 다듬고 싶거든.
     너가 디자인 다듬어주려면 어떤 식으로 해야 하냐?
     외부 디자인 자료(예: getdesign.md 같은 사이트)도 활용 가능해?"
    클로드코드가 5 카테고리(색상·여백·타이포·애니메이션·반응형) +
       외부 리소스 활용(getdesign.md 같은 사이트 → WebFetch로 가져옴) 펼침.

  STEP 3. 2단계 기획하기 (U) — "외부 디자인 시스템에서 무드 고르기"
    "https://getdesign.md/ 에서 70개 디자인 시스템(Vercel·Stripe·Figma 등)이 있는데,
     우리 김지수 기획서·리서치 무드와 잘 맞는 거 추천해줘. 카테고리별로 3-5개 비교.
     WebFetch로 사이트 직접 보고."
    클로드코드가 WebFetch로 getdesign.md 가져옴 → 옵션 비교 (Vercel 미니멀 / Linear 깔끔 / Stripe 컬러 등) +
       김지수 무드와 매칭되는 추천 1-2개 펼침.

  STEP 4. 3단계 만들기 (I) — 디자인 MD 가져와서 적용
    "그중 [Vercel] design.md 다운받아서 우리 portfolio.html에 적용해줘.
     한 번에 다 적용하지 말고 색상부터. 끝나면 다음."
    클로드코드가 design.md 가져와서 카테고리 하나씩 적용 → 브라우저 강력 새로고침으로 확인.
    3개 완료 후: "지금 상태를 portfolio-final.html로 저장해줘."

  STEP 5. 4+5단계 검토 + 부분 수정 (L+D)
    open portfolio-final.html → 체크: 3+ 카테고리 적용? 모바일 깨짐? 본인 의도 부합?
    부분 수정: "Hero 섹션 폰트만 더 굵게(900). 다른 건 그대로"

■ 시작
1. 영상 시작 → STEP 1부터 따라가기
2. 영상 끝나면 이 채팅창에 "완료" 또는 "/wrap" 입력 → 결과물 자동 정리

※ 부분 수정 원칙: "위치 + 수치 + 유지" 명시. "다른 건 그대로" 한 줄 필수.
```

---

## 자유 실습 (스킬 SLEEP)

**WRAP 트리거**: `완료` / `/wrap` / `끝` / `다음 클립`
**도움 요청 트리거**: `막혔어요` / `도와줘`

### 도움 안내

```
어디서 막히셨어요?

  • portfolio.html 못 찾음 → Clip 8 폴더에서 cp 다시 실행
  • STEP 2에서 클로드코드가 바로 수정 시작 → "결과 말고 어떤 식으로 다듬을 수 있는지 흐름부터" 재요청
  • STEP 3에서 기획서 못 읽음 → "01-포트폴리오-구성기획.md 읽어서 우선순위 추천" 명시
  • 변화가 브라우저에 안 보임 → 강력 새로고침 (Mac: Cmd+Shift+R / Win: Ctrl+F5)
  • 수정 지시했는데 다른 부분도 바뀜 → "이 부분만. 다른 건 그대로" 명시 누락
  • HEX 코드 모름 → "따뜻한 오프화이트 HEX 3개 추천해줘" 식으로 AI에 물어봐도 됨
  • 모바일 글자 잘림 → "고정 width 대신 max-width. 모바일 16px 좌우 패딩"

여전히 막히면 더 구체적으로 알려주세요.
```

---

## WRAP

### 1. 파일 검증

`50-my-work/Part03-체험하기/실습12-포트폴리오수정/` 안에 점검:
- `portfolio-final.html` 존재
- 5 카테고리 중 3+ 변경 흔적 (색상·여백·타이포·애니메이션·반응형)

`portfolio-final.html` 없으면 "STEP 4 저장 안 했어요. 지금 저장할까요?" 확인.

### 2. README.md 자동 작성

```markdown
# 실습 12 — 포트폴리오 수정 (다듬기)

- 완료: {ISO 8601}
- 모델: {claude --version}
- 모드: {Auto / Accept-edits / Default}

## 진행 방식
영상 보면서 실습 (STEP 1~5).

## 5단계 사이클
- 1 질문하기 (B): "어떻게 다듬을 수 있어?"
- 2 기획하기 (U): PRD 읽혀서 우선순위 + 옵션 받아 고르기
- 3 만들기 (I): 카테고리 하나씩 차례로 적용
- 4 검토하기 (L): 카테고리 적용·깨짐 없음·의도 부합
- 5 개선하기 (D): "이 부분만 ··· 다른 건 그대로"

## 적용한 카테고리
{적용한 카테고리 목록}

## 결과물
- portfolio-final.html (5 카테고리 중 3+ 적용 완료본)

## 핵심 발견
{자유 입력한 한 줄}

## 다음 클립 연결
Clip 10 (Vercel 배포) — portfolio-final.html을 실제 인터넷 주소로 공개.
```

### 3. progress.json 업데이트

```json
{
  "practice_completed": [..., "실습 12"],
  "current_clip": null,
  "last_activity": "{ISO 8601}"
}
```

### 4. 다음 안내

```
✓ 실습 12 (포트폴리오 수정) 완료.
  결과는 50-my-work/Part03-체험하기/실습12-포트폴리오수정/에 저장됨.

다음은 Clip 10 (Vercel 배포) — portfolio-final.html을 진짜 인터넷 주소로.
이어서 진행하려면 /part03 → Clip 10 선택.
```

---

## EDGE CASES

| 상황 | 대응 |
|---|---|
| portfolio.html 없음 | "Clip 8 폴더에서 복사하세요" + cp 명령 |
| STEP 2 생략 → 바로 수정 | 자유 영역. 결과 차이 확인 |
| 변화 반영 안 됨 | 강력 새로고침 권장 |
| 카테고리 1-2개만 적용 | "3가지 이상 권장. 추가할까요?" 한 번 확인 |
| 부분 수정인데 전체 다시 씀 | "다른 건 그대로" 누락 |
| `완료` 입력 후 final 파일 없음 | "STEP 4 저장 안 했어요. 지금 저장할까요?" |
