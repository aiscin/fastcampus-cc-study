# Clip 5: 자료 리서치 — 주제 한 줄로 자료 수집

> Part 03 / Ch 03 / 실습 8 / ~20분
> 패턴: 자동 셋업 → 영상 보면서 같이 따라하기 → "완료" → WRAP
> 대응 클립: `30-practice-design/part-03-체험하기/clip-05-자료리서치.md`
> 모범답안: `50-my-work/Part03-체험하기/실습08-자료리서치/p3-c04-따라하기.md`

---

## 자동 셋업 (진행 안내 출력 직전 스킬이 즉시 Bash로 실행)

```bash
# 1) 워크스페이스 루트 자동 감지
WS="$(pwd)"
while [ "$WS" != "/" ] && [ ! -f "$WS/CLAUDE.md" ]; do WS="$(dirname "$WS")"; done
if [ "$WS" = "/" ]; then
  WS="$(pwd)"
  while [ "$WS" != "/" ] && [ ! -d "$WS/40-mock-data" ]; do WS="$(dirname "$WS")"; done
fi
[ "$WS" = "/" ] && { echo "⚠ 워크스페이스 루트 못 찾음"; exit 1; }
echo "워크스페이스: $WS"

# 2) 작업 폴더 (Ch 03 시작 — 재료 없음. 폴더만 만들고 시작)
DEST="$WS/50-my-work/Part03-체험하기/실습08-자료리서치"
mkdir -p "$DEST"
cd "$DEST"

echo "=== 결과 ==="
ls -la
```

**셋업 결과 한 줄 안내**: "✓ 실습08-자료리서치 폴더 준비 완료. Ch 03 시작이라 재료 없음 — 주제 한 줄로 시작합니다."

---

## 진행 안내 (스킬이 한 메시지로 출력)

```
Clip 5 — 자료 리서치를 시작합니다. (Ch 03 시작)

■ 오늘 할 거
주제 한 줄 →
  • 02-주제리서치.md (5개 항목 + 출처 URL)
한 사이클로 만듭니다.

Ch 02는 데이터(CSV)가 재료였어요. Ch 03은 재료가 없는 상태에서 시작합니다.
머릿속에 주제 한 줄만 있어요. 그걸로 콘텐츠까지 갑니다.

■ 진행 방식
영상에서 STEP 1~5 순서대로 실습이 진행됩니다.
영상 일시정지·재생 자유롭게 하면서 본인 워크스페이스에서 같이 따라하세요.

■ STEP 흐름

  STEP 1. 작업 폴더 진입 (스킬이 폴더 자동 준비 완료)
    이미 셋업된 폴더: 50-my-work/Part03-체험하기/실습08-자료리서치/
    그대로 이 채팅창에서 STEP 2부터 진행하면 됨.

  STEP 2. 1단계 질문하기 (B) — "검색 시키려면 어떻게?"
    "클로드코드한테 인터넷 검색을 시키려고 하는데
     어떻게 해야 하냐? 어떤 방법들이 있어?"
    클로드코드가 두 도구 차이 펼침: WebSearch(키워드 검색) vs WebFetch(특정 URL 가져오기).

  STEP 3. 2단계 기획하기 (U) — "본인 주제로 어떻게?"
    "좋아. 그럼 26년 AI 주요 이슈 5개를 조사해 오려면
     각 방법으로 어떻게 검색·정리하면 좋을지 옵션 보여줘.
     검색 키워드 후보랑 정리 형식도 같이."
    클로드코드가 검색 키워드 후보 + 정리 형식 옵션(보고서형 / 카테고리 / 타임라인) 펼침.

  STEP 4. 3단계 만들기 (I) — 리서치 실행
    "그 방법으로 26년 AI 주요 이슈 5개 정리해줘.
     각 항목에 핵심 2-3줄 + 출처 URL 1-2개.
     추측 말고 실제 검색 결과 기반으로만.
     결과는 02-주제리서치.md."
    클로드코드가 WebSearch 다회 실행 → MD 파일 생성. 허락 요청 시 Y.

  STEP 5. 4+5단계 검토 + 부분 수정 (L+D)
    체크: 출처 URL 실제로 열리나? 5개 항목 모두 26년 시점 정확? 본인 의도 부합?
    부분 수정 (하나 골라):
      A. 항목 보강: "3번 항목 정보 부족해. 더 자세히. 다른 건 그대로"
      B. 출처 교체: "1번 출처 공식 정보 아닌 듯. 공식 사이트로. 다른 건 그대로"
      C. 구조 개선: "각 항목 끝에 '이 이슈의 영향' 1줄 추가. 다른 건 그대로"

■ 시작
1. 영상 시작 → STEP 1부터 따라가기
2. 영상 끝나면 이 채팅창에 "완료" 또는 "/wrap" 입력 → 결과물 자동 정리
```

---

## 자유 실습 (스킬 SLEEP)

**WRAP 트리거**: `완료` / `/wrap` / `끝` / `다음 클립`
**도움 요청 트리거**: `막혔어요` / `도와줘`

### 도움 안내

```
어디서 막히셨어요?

  • WebSearch 안 됨 → 클로드코드가 WebSearch 허락 요청 시 Y로 진행
  • STEP 2에서 바로 검색 시작 → "검색 말고 어떤 식으로 가능한지 흐름부터" 재요청
  • STEP 3에서 주제 후보 1-2개만 옴 → "주제 후보 4-5개로, 각각 어떤 항목 구조 어울릴지" 재요청
  • 출처 URL 안 들어감 → "각 항목마다 출처 URL 1-2개 명시 추가. 다른 건 그대로"
  • 외국 자료만 옴 → "한국어 자료 위주로 다시 검색"
  • 부분 수정인데 전체 다시 씀 → "다른 건 그대로" 범위 명시 누락

여전히 막히면 더 구체적으로 알려주세요.
```

---

## WRAP

### 1. 파일 검증

`50-my-work/Part03-체험하기/실습08-자료리서치/` 안에 점검:
- `02-주제리서치.md` 존재
- 5개 항목 각각에 출처 URL 포함

URL 없으면 "각 항목에 출처 URL 추가할까요?" 한 번 확인.

### 2. README.md 자동 작성

```markdown
# 실습 8 — 자료 리서치 (주제 한 줄 → 리서치 MD)

- 완료: {ISO 8601}
- 모델: {claude --version}
- 모드: {Auto / Accept-edits / Default}

## 진행 방식
영상 보면서 실습 (STEP 1~5).

## 5단계 사이클
- 1 질문하기 (B): "자료 리서치 어떻게 할 수 있어?"
- 2 기획하기 (U): 주제 후보 + 항목 구조 옵션 받아 고르기
- 3 만들기 (I): WebSearch → 02-주제리서치.md (5개 항목 + 출처 URL)
- 4 검토하기 (L): 출처 검증·항목 관련성·근거 일관성
- 5 개선하기 (D): "N번 항목만 출처 교체. 다른 건 그대로"

## 선택한 주제
{선택한 주제}

## 결과물
- 02-주제리서치.md (5개 항목 + 출처 URL)

## 핵심 발견
{자유 입력한 한 줄}

## 다음 클립 연결
Clip 6 (카드뉴스) — 이 02-주제리서치.md를 10장 HTML 카드뉴스로.
```

### 3. progress.json 업데이트

```json
{
  "practice_completed": [..., "실습 8"],
  "current_clip": null,
  "last_activity": "{ISO 8601}"
}
```

### 4. 다음 안내

```
✓ 실습 8 (자료 리서치) 완료.
  결과는 50-my-work/Part03-체험하기/실습08-자료리서치/에 저장됨.

다음은 Clip 6 (카드뉴스) — 이 리서치를 10장 HTML 카드뉴스로.
이어서 진행하려면 /part03 → Clip 6 선택.
```

---

## EDGE CASES

| 상황 | 대응 |
|---|---|
| WebSearch 안 됨 | "WebSearch 허락 프롬프트 놓쳤을 수 있음. 재시도 + Y 응답" |
| STEP 2 생략 → 바로 검색 시작 | 자유 영역. 차이 확인 |
| 외국 자료 위주 | "한국어 자료만, 국내 기준" 추가 재요청 |
| 출처 URL 안 열림 | "모든 출처 URL 실제로 확인하고 안 열리면 교체. 다른 건 그대로" |
| 5분 이상 지연 | "검색 횟수 줄여도 됨. 항목당 1-2회로 제한" |
| 부분 수정인데 전체 다시 씀 | "다른 건 그대로" 누락 |
| `완료` 입력 후 MD 없음 | "어느 STEP에서 멈췄나요?" 확인 |
