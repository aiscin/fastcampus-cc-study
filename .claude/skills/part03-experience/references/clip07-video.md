# Clip 7: 리모션 영상 — 카드뉴스를 영상으로 변환

> Part 03 / Ch 03 / 실습 10 / ~20분
> 패턴: 자동 셋업 → 영상 보면서 같이 따라하기 → "완료" → WRAP
> 대응 클립: `30-practice-design/part-03-체험하기/clip-07-리모션영상.md`
> 모범답안: `50-my-work/Part03-체험하기/실습10-리모션영상/p3-c06-따라하기.md`

---

## 자동 셋업 (진행 안내 출력 직전 스킬이 즉시 Bash로 실행)

```bash
# 1) 워크스페이스 루트 자동 감지
WS="$(pwd)"
while [ "$WS" != "/" ] && [ ! -f "$WS/CLAUDE.md" ]; do WS="$(dirname "$WS")"; done
if [ "$WS" = "/" ]; then
  WS="$(pwd)"
  while [ "$WS" != "/" ] && [ ! -d "$WS/40-mock-data" ]; do WS="$(dirname "$WS")"; done
fi
[ "$WS" = "/" ] && { echo "⚠ 워크스페이스 루트 못 찾음"; exit 1; }
echo "워크스페이스: $WS"

# 2) 경로
DEST="$WS/50-my-work/Part03-체험하기/실습10-리모션영상"
SRC="$WS/50-my-work/Part03-체험하기/실습09-카드뉴스"

mkdir -p "$DEST"
cd "$DEST"

# 3) Clip 6 결과물(02-카드뉴스.html) 통째 복사
if [ -d "$SRC" ]; then
  cp -r "$SRC"/* ./ 2>/dev/null
  rm -f README.md
fi

# 4) Node.js 버전 체크
node --version 2>/dev/null || echo "⚠ Node.js 미설치 — nvm install 20 권장"

echo "=== 결과 ==="
ls -la
```

**셋업 결과 한 줄 안내**:
- 02-카드뉴스.html 있고 Node 20+ → "✓ 실습10-리모션영상 폴더 준비 완료. Clip 6에서 만든 02-카드뉴스.html 가져왔어요. Node {버전} 확인됨."
- HTML 없음 → "⚠ Clip 6 결과물 없네요. 먼저 Clip 6(카드뉴스)부터 진행해주세요."
- Node 미설치 → "⚠ Node.js 미설치. nvm install 20 안내."

---

## 진행 안내 (스킬이 한 메시지로 출력)

```
Clip 7 — 리모션 영상을 시작합니다. (Ch 03 마지막)

■ 오늘 할 거
Clip 6의 02-카드뉴스.html →
  • Playwright로 브라우저 자동 제어
  • 카드 1장씩 3초씩 머무르며 스크롤
  • output.mp4 (30초 영상)
한 사이클로 만듭니다.

■ 진행 방식
영상에서 STEP 1~5 순서대로 실습이 진행됩니다.
영상 일시정지·재생 자유롭게 하면서 본인 워크스페이스에서 같이 따라하세요.

■ STEP 흐름

  STEP 1. 작업 폴더 진입 (스킬이 폴더+02-카드뉴스.html+Node 체크 자동 완료)
    이미 셋업된 폴더: 50-my-work/Part03-체험하기/실습10-리모션영상/
    그대로 이 채팅창에서 STEP 2부터 진행하면 됨.

  STEP 2. 1단계 질문하기 (B) — "클로드코드로 영상 만들기 어떤 방법?"
    "클로드코드로 영상 만드는 방법 어떤 게 있어?
     어떤 도구로 어떻게 만드는지 알려줘."
    클로드코드가 두 갈래 펼침:
      ① MCP 서버 연결 (Runway / Kling / Replicate — 텍스트→영상 새로 생성)
      ② 자동화 스크립트 (Playwright / Remotion / FFmpeg / MoviePy — 기존 자료 영상화)

  STEP 3. 2단계 기획하기 (U) — "자동화 쪽으로 좁히고 단계별 흐름"
    "그중 자동화 스크립트 쪽으로 가자.
     이 02-카드뉴스.html을 30초 정도 SNS 릴스/쇼츠용으로 빠르게 만들고 싶은데
     어떤 도구가 좋고, 그 도구로 어떻게 진행될지 단계별 흐름 보여줘."
    클로드코드가 Playwright 추천 + 4단계 흐름 (설치 → 스크립트 → 실행 → 확인) 펼침.

  STEP 4. 3단계 만들기 (I) — 스크립트 작성 + 실행
    "가장 빠른 방법으로 가자.
     Playwright로 브라우저 자동 제어 → 카드 1장씩 3초씩 머무르며 스크롤 → MP4로 녹화.
     결과 파일: output.mp4. 스크립트 파일 작성하고 실행까지."
    Playwright 설치 허락 요청 시 Y → 스크립트 작성 + 실행 → output.mp4 생성.

  STEP 5. 4+5단계 검토 + 부분 수정 (L+D)
    open output.mp4 → 체크: 길이 20~40초 / 카드 한 장씩 / 끊김 없음
    부분 수정: "각 카드 3초 → 5초로 늘려. 다른 건 그대로"

■ 시작
1. 영상 시작 → STEP 1부터 따라가기
2. 영상 끝나면 이 채팅창에 "완료" 또는 "/wrap" 입력 → 결과물 자동 정리

※ Node.js 20+ 필요. Playwright·ffmpeg 설치 허락 요청 뜨면 Y로 진행.
```

---

## 자유 실습 (스킬 SLEEP)

**WRAP 트리거**: `완료` / `/wrap` / `끝` / `다음 클립`
**도움 요청 트리거**: `막혔어요` / `도와줘`

### 도움 안내

```
어디서 막히셨어요?

  • 02-카드뉴스.html 못 찾음 → cp ../실습09-카드뉴스/02-카드뉴스.html ./ 다시 실행
  • Node.js 버전 낮음 → "nvm install 20 → nvm use 20" 안내
  • STEP 2에서 클로드코드가 바로 실행 → "결과 말고 어떻게 할 수 있는지 흐름부터" 재요청
  • STEP 3에서 형태 비교 없이 추천만 옴 → "MP4·GIF·릴스·Remotion 표로 비교해줘" 재요청
  • Playwright Chromium 다운로드 실패 → "이미 설치된 Chrome 사용하도록 수정"
  • MP4 까만 화면 → "각 카드 대기시간 5초로, 페이지 로드 완료 대기 충분히 잡아줘"
  • 부분 수정인데 스크립트 전체 다시 씀 → "N초만 바꿔. 다른 건 그대로" 명시 누락

여전히 막히면 더 구체적으로 알려주세요.
```

---

## WRAP

### 1. 파일 검증

`50-my-work/Part03-체험하기/실습10-리모션영상/` 안에 점검:
- `output.mp4` 존재 + 길이 20~40초

파일 없으면 "STEP 4 실행 안 했어요. 지금 만들까요?" 한 번 확인.

### 2. README.md 자동 작성

```markdown
# 실습 10 — 리모션 영상 (카드뉴스 → MP4)

- 완료: {ISO 8601}
- 모델: {claude --version}
- 모드: {Auto / Accept-edits / Default}

## 진행 방식
영상 보면서 실습 (STEP 1~5).

## 5단계 사이클
- 1 질문하기 (B): "영상 어떻게 만들 수 있어?"
- 2 기획하기 (U): 형태 비교 받아서 고르기 (MP4 Playwright 선택)
- 3 만들기 (I): 스크립트 작성 + 실행 → output.mp4
- 4 검토하기 (L): 길이·일관성·의도 부합
- 5 개선하기 (D): "카드 N초로. 다른 건 그대로"

## 결과물
- output.mp4 (~30초)

## 핵심 발견
{자유 입력한 한 줄}

## 다음 클립 연결
Ch 03 종료 — 주제 한 줄에서 영상까지 코드 한 줄도 직접 안 침.
다음 Clip 8 (포트폴리오 기획) — Ch 04 시작. 새 폴더에서 진행.
```

### 3. progress.json 업데이트

```json
{
  "practice_completed": [..., "실습 10"],
  "completed_chapters": [..., "Ch 03"],
  "current_clip": null,
  "last_activity": "{ISO 8601}"
}
```

### 4. 다음 안내

```
✓ 실습 10 (리모션 영상) 완료. Ch 03 종료.
  결과는 50-my-work/Part03-체험하기/실습10-리모션영상/에 저장됨.

다음은 Clip 8 (포트폴리오 기획) — Ch 04 시작. 새 폴더에서 진행.
이어서 진행하려면 /part03 → Clip 8 선택.
```

---

## EDGE CASES

| 상황 | 대응 |
|---|---|
| 02-카드뉴스.html 없음 | "Clip 6 폴더에서 복사하세요" + cp 명령 안내 |
| Node.js 버전 낮음 | "nvm install 20" 안내 |
| STEP 2 생략 → 바로 만들기 | 자유 영역. 결과 차이 확인 |
| Playwright 설치 막힘 | AI 허락 요청 시 Y |
| MP4 너무 빠름/느림 | "카드 N초로 조정. 다른 건 그대로" |
| GIF 너무 큼 | "gifsicle 또는 ffmpeg fps 12로 최적화" |
| 부분 수정인데 전체 다시 씀 | "다른 건 그대로" 누락 |
| `완료` 입력 후 output.mp4 없음 | "어느 STEP에서 멈췄나요?" 확인 |
