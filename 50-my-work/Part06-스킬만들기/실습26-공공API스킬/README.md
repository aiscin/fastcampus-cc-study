# 실습 26 — 공공 API 스킬 (TourAPI 여행 가이드)

| 항목 | 내용 |
|---|---|
| 완료 시각 | 2026-06-24 |
| Part / Clip | Part 06 / Clip 5 |
| 모델 | Claude Opus 4.8 |
| 만든 스킬 | **trip-advisor** (`.claude/skills/trip-advisor/`) |
| 시연 | 구미 2박3일 부부 여행 가이드 (사진·메뉴·체크인·동선 지도) |

## 무엇을 만들었나

블로그·유튜브에서 여행 코스를 모으고 → **공공데이터포털 TourAPI(KorService2)**로 검증·정형화 → 전문가 4관점 교차검증 → **동선 지도**까지 그려 날짜별 여행 가이드를 만드는 스킬.

### 스킬 구성
```
trip-advisor/
├── SKILL.md                       # 0인터뷰→1수집→2검증→3보강→4배치→4.5지도→5초안→6전문가검증→7저장
├── references/
│   ├── research-method.md         # 딥리서치 차용 수집법 (날짜인식·하위주제·교차검증·A~E등급)
│   └── expert-verification.md     # 전문가 4인(정확성·동선·광고편향·운영) refute 검증
└── scripts/
    ├── tour_api.py                # 6액션: area/food/stay/detail/intro/images (시군구 자동)
    ├── collect_places.py          # 블로그×API 융합·신뢰도 점수·실패 격리
    └── make_map.py                # 좌표→OSM 타일 합성 동선 지도 PNG
```

### 시연 산출물
- `구미-여행가이드-20260624.md` — 사진·메뉴·체크인·계절·축제 + 동선 지도 임베드
- `구미-동선지도-20260624.png` — Day별 색 동선 + 번호 마커
- `verify-report-구미-20260624.md` — 4관점 검증 기록 (BLOCK 2건 → 수정 → 통과)

## 메타 흐름 (그냥 만들지 않았다)
1. "어떻게 워크플로우를 구성해야 할까?"로 출발 → 단계 정의
2. **AI에게 맡길 것(판단·글쓰기) vs 코드로 뺄 것(API·점수·지도)** 분리
3. 3개 도구 협업: **딥리서치**(수집법) → **끼리끼리**(검증 단계) → **스킬러들의 수다**(4전문가 분석·개선)
4. 실전 2회로 검증 — 검증 단계가 동선 BLOCK을 실제로 잡아냄

## Part 5 자산 사슬
- `.env`(TourAPI 키) ← 실습 21 trash-guard hook 이 안전 보관
- `.gitignore` 의 `.env` ← API 키 깃 유출 방지
- 실습 22 GitHub 백업 ← 만든 스킬 한 묶음 커밋

## 본인 일 응용 (공공데이터포털)
미세먼지·부동산 실거래·환율·대중교통 등 다른 공공 API도 같은 패턴(스크립트 분리 + 검증)으로 스킬화 가능.
